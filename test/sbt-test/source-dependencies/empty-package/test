> setup; reload

$ copy-file changes/Define1.scala Define.scala
$ copy-file changes/Use.scala Use.scala
> compile

$ copy-file changes/Define2.scala Define.scala
> compile

$ delete Define.scala
# TODO this compilation should fail. it used to fail thanks to the default
# `withRecompileAllFraction`, but with that set to `1.0` compilation no longer fails.
> compile

$ copy-file changes/Define1.scala Define.scala
> compile
